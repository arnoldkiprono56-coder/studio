'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function StaffPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Staff Management</CardTitle>
                <CardDescription>Manage Admins and Assistants.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>Staff management interface will be built here.</p>
            </CardContent>
        </Card>
    );
}
