'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function PredictionsPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Prediction Logs</CardTitle>
                <CardDescription>View all predictions generated by users.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>Prediction log interface will be built here.</p>
            </CardContent>
        </Card>
    );
}
